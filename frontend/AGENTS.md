# frontend Agent Notes

This is a placeholder for future frontend implementation.

Current state:

- No frontend framework is selected in repository files.
- No package manager config exists.
- `README.md` says the first phase focuses on the backend auth skeleton.

Do not introduce a frontend stack until the user selects the next runnable slice. When implementation starts, update [../docs/architecture.md](../docs/architecture.md), [../docs/agent/context-map.md](../docs/agent/context-map.md), and [../docs/doc-sync-map.tsv](../docs/doc-sync-map.tsv).
